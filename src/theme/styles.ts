import { mode, Styles, SystemStyleFunction } from "@chakra-ui/theme-tools";
import { CSSObject } from "@emotion/react";

const styles: Styles = {
  global: (props: any) => ({}),
};

export default styles;
